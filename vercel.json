{
  "version": 2,

  "buildCommand": "npm run vercel-build",

  "outputDirectory": "dist/client",

  "routes": [
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*)", "dest": "/api/_worker" }
  ],

  "functions": {
    "api/_worker.*": {
      "runtime": "edge"
    }
  }
}